import React from 'react';
import { Shield } from 'lucide-react';

const IconComponent = () => {
  return (
    <div className="relative w-8 h-8">
      {/* Decorative background shields */}
      <div className="absolute inset-0 rotate-45">
        <Shield 
          size={32}
          strokeWidth={1.5}
          className="absolute inset-0 text-blue-600 opacity-5"
        />
      </div>
      <div className="absolute inset-0 -rotate-45">
        <Shield 
          size={32}
          strokeWidth={1.5}
          className="absolute inset-0 text-blue-600 opacity-5"
        />
      </div>

      {/* Secondary shield with glow effect */}
      <Shield 
        size={32}
        strokeWidth={2}
        className="absolute inset-0 text-blue-400 opacity-20 blur-[0.5px]"
      />
      
      {/* Main shield with gradient effect */}
      <div className="absolute inset-0 bg-gradient-to-br from-blue-500 to-blue-700 mix-blend-soft-light opacity-30" />
      
      {/* Primary shield */}
      <Shield 
        size={32}
        strokeWidth={2}
        className="absolute inset-0 text-blue-600"
      />

      {/* Highlight overlay */}
      <div className="absolute inset-0 bg-gradient-to-tr from-transparent via-blue-200 to-transparent opacity-10" />
    </div>
  );
};

export default IconComponent;